# =============================================================================
# TaskFlow Docker Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your production values
# 
# Usage:
#   cp .env.example .env
#   nano .env  # Edit with your values
#   docker-compose up -d --build
# =============================================================================

# -----------------------------------------------------------------------------
# MongoDB Configuration (Atlas or External)
# -----------------------------------------------------------------------------
# Format for MongoDB Atlas:
#   mongodb+srv://USERNAME:PASSWORD@cluster.xxxxx.mongodb.net/DATABASE
# Format for self-hosted:
#   mongodb://USERNAME:PASSWORD@hostname:27017/DATABASE

MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/taskflow?retryWrites=true&w=majority
MONGODB_DB_NAME=taskflow

# -----------------------------------------------------------------------------
# Authentication Secrets
# -----------------------------------------------------------------------------
# CRITICAL: Generate secure secrets for production!
# Generate with: openssl rand -base64 32

AUTH_SECRET_USER=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MINIMUM_32_CHARACTERS

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# These are typically overridden in docker-compose.yml
# but can be customized here if needed

NODE_ENV=production
PORT=3000
BASE_URL=http://server:3000

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# For production, update with your actual domain

CORS_ORIGINS=http://localhost,https://yourdomain.com
TRUSTED_ORIGINS=http://localhost,https://yourdomain.com